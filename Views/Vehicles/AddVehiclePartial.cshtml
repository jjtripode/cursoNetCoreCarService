@model AddVehicleViewModel

<button type="button" class="btn btn-primary" data-toggle="modal" data-target="#exampleModalCenter">
    Agregar
</button>
<form asp-action="AddCar" method="POST">
    <div class="modal fade" id="exampleModalCenter" tabindex="-1" role="dialog" aria-labelledby="exampleModalCenterTitle" aria-hidden="true">
        <div class="modal-dialog modal-dialog-centered" role="document">
            <div class="modal-content">
                <div class="modal-header">
                    <h5 class="modal-title" id="exampleModalLongTitle">Modal title</h5>
                    <button type="button" class="close" data-dismiss="modal" aria-label="Close">
                        <span aria-hidden="true">&times;</span>
                    </button>
                </div>
                <div class="modal-body">

            <div class="col-sm-6">
                <label asp-for="BrandId" class="control-label">Modelo:</label>
                <select asp-for="BrandId" asp-items="@(new SelectList(Model.Brands,"Id","Descripcion"))" class="form-control"></select>
            </div>
            <div class="col-sm-6">
                <label asp-for="ModelId" class="control-label">Modelo:</label>
                <select asp-for="ModelId" asp-items="@(new SelectList(Model.Models,"Id","Descripcion"))" class="form-control"></select>
            </div>

                    <div class="form-group">
                        <label for="ManufacturingDate" asp-for="ManufacturingDate" class="col-form-label">Fecha Fabricacion</label>
                        <input asp-for="ManufacturingDate">
                    </div> -->

                </div>
                <div class="modal-footer">
                    <button type="button" class="btn btn-secondary" data-dismiss="modal">Close</button>
                    <button type="submit" class="btn btn-primary">Guardars</button>
                </div>
            </div>
        </div>
    </div>
</form>